# Application Name
spring.application.name=talentsync-api

# Server Configuration
server.port=${SERVER_PORT:8080}
server.servlet.context-path=/api/v1

# Database Configuration - Environment-based for Docker
spring.datasource.url=${DATABASE_URL:jdbc:postgresql://dpg-d2q36n15pdvs73djn320-a.oregon-postgres.render.com/talensyncdatabase}
spring.datasource.username=${DATABASE_USERNAME:talensyncdatabase_user}
spring.datasource.password=${DATABASE_PASSWORD:rK8PDbh38AO7dmm9omjmKpuzR0HA7Kwr}
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA/Hibernate Configuration
spring.jpa.hibernate.ddl-auto=validate
spring.jpa.show-sql=${HIBERNATE_SHOW_SQL:false}
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# Flyway Migration Configuration
spring.flyway.enabled=true
spring.flyway.baseline-on-migrate=true
spring.flyway.locations=classpath:db/migration

# Security Configuration
application.security.jwt.secret-key=${JWT_SECRET_KEY:8cb9081783eb103ccf1aa01b182c6d62f0d3f249e8419d4493d7e9a74f2ddac6}
application.security.jwt.expiration=${JWT_EXPIRATION:86400000}
application.security.jwt.refresh-token.expiration=${JWT_REFRESH_EXPIRATION:604800000}

# CORS Configuration
cors.allowed-origins=${CORS_ALLOWED_ORIGINS:http://localhost:3000,http://localhost:5173,https://talentsync.netlify.app}

# Logging Configuration
logging.level.root=${LOG_LEVEL:INFO}
logging.level.com.talentsync=DEBUG
logging.level.org.springframework.web=INFO
logging.level.org.hibernate=INFO

# OpenAI Configuration
openai.api.key=${OPENAI_API_KEY:your_default_key_for_development}
openai.model=${OPENAI_MODEL:gpt-4-turbo}
